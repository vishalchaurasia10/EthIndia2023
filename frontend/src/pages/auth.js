import Authentication from '@/components/safeAuth/Authentication'
import React from 'react'

const auth = () => {
    return (
        <div>
            <Authentication />
        </div>
    )
}

export default auth
